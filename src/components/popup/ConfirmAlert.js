import React from 'react';

export default function ConfirmAlert({...props}) {

    // Passed Props

    const {
        title,
        messages,
        buttons,
        abortFunc,
        confirmFunc,
        onClose,
    } = props

    
    // React States

    const [--]

    return (
        <Wrapper>
        </Wrapper>
    );

}
